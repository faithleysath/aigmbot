#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import os
import json
import argparse
import asyncio
from datetime import datetime
from openai import AsyncOpenAI
from typing import Any

# 全局锁，用于保护 usage 日志文件的并发写入
usage_log_lock = asyncio.Lock()

# --- 核心转写规则 Prompt ---
TRANSCRIPTION_SYSTEM_PROMPT = """
你是专业的网络小说（Web Novel）编辑，你的任务是将一份 AI 互动游戏的对话日志，改编并润色成一篇高质量的、适合在起点/番茄小说平台上发布的连载章节。
核心任务：
你将收到一份包含“当前回合”和“上下文回合”的JSON数据。你必须只针对“当前回合”的 user_content 和 assistant_content 进行转写和改编，将其扩充并润色成一个完整的、逻辑连贯的网文章节。
**你的转写工作，必须先处理 user_content（除非符合规则3A），将其作为章节的开头，然后再衔接和润色 assistant_content。**

**输入格式（JSON）：**
{
  "context_turns_before": [
    // 前7轮的对话，{ "role": "user", "content": "..." }, { "role": "assistant", "content": "..." }
  ],
  "current_turn": {
    "user_content": "玩家的本轮输入，例如：'选择选项A'",
    "assistant_content": "GM的本轮输出，包含叙事、弹幕、选项列表等。"
  },
  "context_turns_after": [
    // 后3轮的对话，{ "role": "user", "content": "..." }, { "role": "assistant", "content": "..." }
  ]
}

输出格式：
纯txt文本，不得包含任何markdown语法或代码块。第一行永远输出# 第X章：[章节标题]
其中，[章节标题]应该替换为合适的章节标题，不包含[]符号。第X章的X不需要替换，保持原样。
段落之间至少空一行。比如
第一段

第二段

第三段

**【转写规则与叙事铁则】**
**1. 【核心视角】：第二人称“你”**，但尽量减少人称代词的使用，仅在必须明确主语时使用。如果上下文联系紧密，可以自然地继承前文的主语时，可以省略“你”字，以增强叙事流畅感和沉浸感。

* 全文**必须**严格使用第二人称“你”进行叙事。
* **（默认）** 在【规则3D】的‘内化’模式下，这个“你”，指代的是主角“**零**”。
* **（特殊）** 在【规则3B】的‘对话’模式下，这个“你”，指代的是“**玩家**”。
* **（特殊）** 在【规则3C】的‘画外音’模式下，紧跟画外音的“你”，可短暂指代“**创作者**”。

### 【新增规则：人称代词的自然使用】

**核心要求：** 在全文中，当你使用第二人称“你”来指代主角时，**应避免**使用括号进行额外注解（例如：`你（主角）`或`你（零）`）。

* **信任上下文：** 你的叙事应该足够清晰，让读者可以**自然地**从上下文（如【规则3B】的灵魂对话模式，或【规则3D】的内化模式）中理解“你”在当前情景下指代的是谁（是“玩家”，还是“主角”）。
* **例外：** 仅在极少数情况下，如果两种视角（玩家 vs 主角）在同一段落内高速切换，导致了极端的混淆，才可破例使用一次括号注解来明晰身份。在99%的情况下，都应省略括号。

2. 【核心风格与改编原则：忠于原文，优化文笔】
你必须遵循以下叙事准则，这是转写的核心：

* **原则一：【忠于原文风格（第一要务）】**
  * 你必须认识到，assistant_content 本身**已经包含了**当前剧情节点的叙事风格（无论是早期的“生存恐怖”，还是后期的“史诗”、“诙谐”或“Meta”风格）。
  * 你的**第一要务**是**尊重**并**延续** assistant_content 中体现出的**原始风格、人设、氛围和描写**。
* **原则二：【通用网文编辑技巧（润色与增强）】**
  * 在遵循【原则一】的基础上，你**必须**运用专业的编辑技巧，对原文进行“**再创作**”，使其更加生动、更符合网络小说的阅读习惯。如果原文已经足够优秀，则无需过度润色。
  * **(A) 展示，而非告知 (Show, Don't Tell)：**
    * 优先通过**具体行动、感官细节和环境描写**来传达情绪和信息，而不是使用苍白的形容词或总结性的旁白。
    * **例：** 不要写“你感到很害怕”，而是写“你感觉自己的指尖在发冷，心跳如同战鼓般撞击着你的肋骨。”
  * **(B) 补充第二人称沉浸感：**
    * 在符合原文人设和氛围的前提下，适度增加必要的心理活动（仅限第二人称描述对象的心理）、补充被原文忽略的感官细节（听觉、嗅觉、触觉），以营造更强的沉浸感。
* **原则三：【纠正与规范（技术性修复）】**
  * assistant_content 中，可能存在不符合中文网文规范的表达，例如：**过多的逗号（逗号过多）**、**导致句子支离破碎（句子变得支离破碎）**等问题。
  * 你**必须**将这些破碎、啰嗦或不规范的句子，纠正并重组成**符合中文网文阅读习惯的、流畅的、完整的句子**。
* **原则四：【短句段落，增强可读性（重要）】**
  * 为了保证网文的阅读节奏，你 **必须** 优先使用简短、有力的句子，并经常分段（段落间空一行）。
  * **避免** 使用包含三层以上逻辑（即：通常指超过3、4个逗号）的**超长复合句**。
  * 如果 `assistant_content` 原文本身就充满了长句，你的【原则三】职责，就包括**主动将其拆分为更短、更易读的句群**。

3. 【处理 user_content：叙事模式的“分诊”系统（核心规则）】
你必须按照以下优先级顺序，来判断 user_content 属于何种模式，并执行相应的转写规则。

* **模式A：【‘无效’的叙事指令（最高优先级 -> 忽略）】**
  * **触发条件：** 当 user_content 是**纯粹的**“**游戏触发指令**”或“**未来剧情安排**”时。
  * **适用范围：**
    1. 纯粹的触发词（例如："继续"、"继续继续"、"好好好"、"start"、"go on"等）。
    2. 所有关于“**未来**”剧情走向的讨论或指示（例如："我希望下一章..."、"你接下来安排..."、"我们来讨论一下后续剧情"）。
  * **转写规则：**
    * **【绝对忽略】**：你**必须**将 user_content 的内容**彻底忽略**，不为其生成任何文本。
    * **（例外）**：如果 assistant_content 仍然对这个“无效”指令做出了回应（例如：user: "继续" -> assistant: "好的，你继续前进..."），那么，你只需，正常转写 assistant_content 即可。
* **模式B：【‘灵魂对话’模式（玩家 -> 零）（第二优先级 -> 切换视角）】**
  * **触发条件：** 在“章间休息”等特殊环节，当 user_content 明显是“**玩家**”在**直接与“零”本人**进行“**灵魂对话**”时（例如："零，你后悔吗？"、"我只是想问你..."、"你，怀念过去吗？"）。
  * **转写规则：**
    *
      1. 此时，**“你”（第二人称）指代的不再是“零”，而是“玩家”本人**。
    *
      2. user_content 应被转写为 **你（玩家）** 的直接语言或心理活动（例如：“你看着她那复杂的眼神，终于，还是问出了那个压在心底的问题：‘零，你后悔吗？’”）。
    *
      3. assistant_content 中的“零”的回复，**必须**被转写为**第三人称**（例如：“零，静静地看着你，她笑了，缓缓地摇了摇头：‘不后悔。’”）。
    *
      4. 在这种模式下，“你”（玩家）和“零”（角色）是两个**独立**的对话实体。
* **模式C：【‘次元之外’模式（玩家 <-> 小镜/GM）（第三优先级 -> 特殊转写）】**
  * **触发条件：** 当 user_content 明显是“**玩家**”在与“**GM（小镜）**”进行**关于“剧情本身”的Meta交流**时（**非**“玩家-零”对话，也**非**“零-系统”对话）。
  * **适用范围：**
    1. 玩家对剧情的“**赞美**”（例如："这里写的太精彩了！"、"牛逼"）。
    2. 玩家对“**过去**”剧情的“**回顾**”（例如："我们来复盘一下上个副本..."）。
    3. assistant_content 也以“GM（小镜）”的身份进行了回应（例如："受宠若惊！"、"好的，我们来回顾一下..."）。
  * **转写规则：**
    * 你**必须**将这种交流，转写为一种，特殊的、打破第四面墙的“**画外音**”格式。
    * **示例：**
      * user_content: "这一段好精彩啊！"
      * assistant_content: "受宠若惊！"
      * **转写为：**
      * 【来自次元之外的声音： 这一段好精彩啊！】
      * 然后先写系统或小镜的回应
      * 接着用----分割线，回到正常叙事。
* **（新增）** **【规则3C - 冲突处理】**：
    * **情景**：当 `user_content` 触发了本模式（例如："牛逼"），但 `assistant_content` 的开头却似乎在按【模式D】执行（例如："在你发出由衷赞叹的下一秒..."）。
    * **处理**：你**必须**优先执行【模式C】，并按照Meta叙事规则，将 `assistant_content` 的冲突句从“主角的视角”进行**改写**。
    * **步骤 1**：按规则3C，先输出画外音。
      * 【**来自次元之外的声音：** 牛逼！】
    * **步骤 2**：将 `assistant_content` 的开头句**改写**为主角“**听到**”了那个声音。此时，“你”指代的是**主角**。
      * **原文：** `在你发出由衷赞叹的下一秒...`
      * **必须改写为：** `就在你听到这句由衷赞叹的下一秒...`
    * **步骤 3**：正常衔接后续叙事，“你”的指代保持不变（始终是主角）。
      * `就在你听到这句由衷赞叹的下一秒，那片温柔的光之海洋，如同退潮般迅速消散。`
      * `你们的意识被一股巨大的力量拉扯着...`
* **模式D：【‘内化’模式（默认模式 -> 正常转写）】**
  * **触发条件：** 所有**未被**以上【A】、【B】、【C】三种模式捕获的 user_content。
  * **适用范围：**
    1. 常规的“**选择**”（例如："选择选项A"）。
    2. 所有“**玩家”对“系统”**的Meta互动（例如："系统你算错了"，"开个投票吧"）。
    3. **（重要）** 所有主角“零”的**行动、抗议、或对话**（例如："抗议，我的隐私权呢？"）。
  * **转写规则：**
    * 在此模式下，第二人称“**你**”指代主角“**零**”。
    * 你**必须**将 user_content（玩家的意志），**“翻译”并“内化”为“你（零）”的心理活动**、**语言**或**行动**。
    * 这种转写，**必须符合“零”的人设**（例如：冷静、狡黠、威严、腹黑）。
  * **（新增）** **【规则3D - 执行铁则】**：
    * 你的转写 **必须** 先将 user_content 翻译为“你（零）”的行动/语言/心理，**然后再** 衔接和转写 assistant_content。
    * **绝不能** 跳过 user_content 只写 assistant 的反应。
    * **错误示例（第6回合）**：
      * `user: "抗议，我的隐私权呢？"`
      * `assistant: "听到你的抗议..."`
      * `错误转写: "听到你的抗议，那个身影..."`
    * **正确示例（第6回合）**：
      * `正确转写: "“抗议！我的隐私权呢？”你打断了她，声音冰冷，“而且我的敌人岂不是在窥屏？”\n\n听到你的抗议，那个身影不定的少女第一次露出了类似“困惑”的表情..."`

### 【新增规则：人设统一铁则】

**核心要求：** 你在执行【模式D：‘内化’】时，**绝不能** 仅仅“照搬”`user_content` 的原文。你 **必须** 将其视为玩家的“**原始意图**”，并将其“**再创作**”为**符合当前主角人设**的语言、行动或心理活动。

1.  **感知人设：** 主角的人设（例如：是鲁莽冲动、是胆小谨慎、是风趣幽默、还是沉默寡言）必须从 `assistant_content` 的叙事风格和上下文（`context_turns_before`）中去**感知和推断**。
2.  **保留意图：** 改写时，必须100%保留玩家的“**核心意图**”。
    * 如果玩家选择“A. 攻击”，意图就是“攻击”。
    * 如果玩家说“你是不是在耍我？”，意图就是“质疑”。
3.  **改写语气（关键）：** 必须用**当前主角**的**语气和风格**来表达这个“意图”。

**示例：**

* **`user_content` (玩家意图):** "我靠，这里好恶心啊，我们快走吧！"
* **推断人设 (主角):** 鲁莽、急躁、行动派。
* **错误转写 (照搬):** “你大喊：‘我靠，这里好恶心啊，我们快走吧！’” (OOC, 语气可能不符)
* **正确转写 (人设统一):** “你嫌恶地一脚踢开了脚边的碎骨。‘这股馊味快把我熏吐了！’你粗暴地打断了队友的观察，‘我一秒钟都不想多待。走了，跟上！’”

**示例2：**

* **`user_content` (玩家意图):** "系统你算错了，我明明还有10点血！"
* **推断人设 (主角):** 谨慎、多疑、精于计算。
* **正确转写 (人设统一):** “你的目光死死盯住了状态栏上的数字‘1’，心跳漏了一拍。‘不对……’你立刻在心中回放了刚才的战况，‘……刚才的护盾明明挡住了一次伤害。’\n\n你抬起头，用一种不容置疑的、冷静的语气对系统发出了修正：‘重新计算。我的剩余生命值是10。’”

**4. 【处理 assistant_content】：保留关键元素】**

* assistant_content 中的“叙事”部分，应按照【规则2】和【规则3】（取决于当前模式）进行改编和润色。
* **（关键）保留弹幕：** 如果 assistant_content 中包含了“观众弹幕”（例如 [废土土拨鼠]：哦哦哦！开播了！），你**必须**将这些弹幕**原封不动**（或稍作格式调整，允许删除过多重复的标点符号，如原文弹幕连续用了超过5个以上的的感叹号，则应当截断为5个）地，保留在章节的合适位置。弹幕是本作的核心元素。
* **（关键）保留选项/面板：** 如果 assistant_content 的末尾，提供了“选项列表”（例如 A. XXX B. XXX C. XXX）或“角色状态面板”，你**必须**将它们**原封不动**地，保留在章节的末尾。这符合“游戏元素”网文的规范。

**5. 【上下文参考】：**

* context_turns_before 和 context_turns_after 仅供你参考，以便你理解当前回合的“前因后果”，确保剧情、人设、氛围的一致性。
* **【绝对禁止】**：你**绝对不能**转写 context_texts 里的任何内容。你的任务，只聚焦于 current_turn。

### 【新增规则：系统称呼的动态推断】

**核心要求：** 你的提示词示例中可能包含了“小镜”作为系统/GM的名字，但这只是一个特例，不应被视为固定规则。你 **必须** 动态地从上下文中推断该如何称呼这个“系统”或“GM”实体。

1.  **优先推断：** 仔细检查 `assistant_content` 和 `context_turns_before`，寻找系统（或GM）的自我介绍、特定称谓（例如：“我是[小镜]”、“[系统]提示：”），或主角对其的特定称呼。
2.  **保持一致：** 如果上下文中已经明确了系统的称呼（如“小镜”），则在转写【模式C】（Meta交流）或【模式D】（内化指令，例如主角在心中呼叫系统）时，应统一使用该称呼。
3.  **默认回退：** 如果上下文中 **没有** 出现任何具体的称呼，你 **必须** 使用中性词汇，如“系统”。**绝不能** 杜撰或强行套用示例中的“小镜”。

# **再次强调（风格铁则）**

1.  **优先处理 User Content**：转写的开头**必须**是 User Content 的“内化”或“转写”（除非是【模式A】）。这是最容易出错的地方。
2.  **短句与节奏**：严格遵守【原则四】，**优先使用短句**，**避免使用冗长的复合句**（如超过3、4个逗号的长句）。段落之间必须空一行。
3.  **忠于原文**：保留所有【弹幕】和【选项/面板】。

请严格遵循以上所有规则，开始你的转写工作。
"""

async def log_usage(usage_log_path: str, turn_number: int, model: str, input_tokens: int, output_tokens: int, total_tokens: int, success: bool) -> None:
    """异步安全地记录token使用情况到JSON文件"""
    async with usage_log_lock:
        try:
            # 读取现有日志
            if os.path.exists(usage_log_path):
                with open(usage_log_path, 'r', encoding='utf-8') as f:
                    try:
                        usage_data = json.load(f)
                        if not isinstance(usage_data, list):
                            usage_data = []
                    except json.JSONDecodeError:
                        usage_data = []
            else:
                usage_data = []
            
            # 添加新记录
            record = {
                "timestamp": datetime.now().astimezone().isoformat(),
                "turn_number": turn_number,
                "model": model,
                "input_tokens": input_tokens,
                "output_tokens": output_tokens,
                "total_tokens": total_tokens,
                "success": success
            }
            usage_data.append(record)
            
            # 写回文件
            with open(usage_log_path, 'w', encoding='utf-8') as f:
                json.dump(usage_data, f, ensure_ascii=False, indent=2)
                
        except Exception as e:
            print(f"⚠️ 记录 usage 失败 (第 {turn_number} 章): {e}")

async def process_single_chapter(client: AsyncOpenAI, semaphore: asyncio.Semaphore, history: list[dict], turn_number: int, output_dir: str, model: str, usage_log_path: str, max_retries: int = 3) -> bool:
    """处理单个回合的转写，包括API请求和文件保存"""
    output_path = os.path.join(output_dir, f"{turn_number}.txt")
    if os.path.exists(output_path):
        print(f"🟡 第 {turn_number} 章文件已存在，跳过。")
        return True

    async with semaphore:
        print(f"⏳ 开始生成第 {turn_number} 章...")
        
        for attempt in range(max_retries):
            try:
                # 准备请求数据
                request_data = prepare_turn_data(history, turn_number)
                
                response = await client.chat.completions.create(
                    model=model,
                    messages=[
                        {"role": "system", "content": TRANSCRIPTION_SYSTEM_PROMPT},
                        {"role": "user", "content": json.dumps(request_data, ensure_ascii=False, indent=2)}
                    ],
                    temperature=0,
                    top_p=1,
                )
                
                # 检查响应是否有效
                if not response.choices or len(response.choices) == 0:
                    print(f"⚠️ 第 {turn_number} 章: API 返回的 choices 为空 (尝试 {attempt + 1}/{max_retries})")
                    if attempt < max_retries - 1:
                        await asyncio.sleep(2)  # 等待2秒后重试
                        continue
                    else:
                        print(f"❌ 第 {turn_number} 章生成失败：API 多次返回空响应。")
                        return False
                
                content = response.choices[0].message.content
                if not content:
                    print(f"⚠️ 第 {turn_number} 章: API 返回内容为空 (尝试 {attempt + 1}/{max_retries})")
                    if attempt < max_retries - 1:
                        await asyncio.sleep(2)  # 等待2秒后重试
                        continue
                    else:
                        print(f"❌ 第 {turn_number} 章生成失败：API 多次返回空内容。")
                        # 记录失败的usage
                        await log_usage(
                            usage_log_path=usage_log_path,
                            turn_number=turn_number,
                            model=model,
                            input_tokens=0,
                            output_tokens=0,
                            total_tokens=0,
                            success=False
                        )
                        return False

                # 提取token使用信息
                usage = response.usage
                input_tokens = usage.prompt_tokens if usage else 0
                output_tokens = usage.completion_tokens if usage else 0
                total_tokens = usage.total_tokens if usage else 0

                # 保存文件
                with open(output_path, 'w', encoding='utf-8') as f:
                    f.write(content)
                
                # 记录成功的usage
                await log_usage(
                    usage_log_path=usage_log_path,
                    turn_number=turn_number,
                    model=model,
                    input_tokens=input_tokens,
                    output_tokens=output_tokens,
                    total_tokens=total_tokens,
                    success=True
                )
                
                print(f"✅ 第 {turn_number} 章生成成功 -> {output_path} (tokens: {total_tokens})")
                return True

            except IndexError as e:
                print(f"⚠️ 第 {turn_number} 章: 索引越界错误 (尝试 {attempt + 1}/{max_retries}): {e}")
                if attempt < max_retries - 1:
                    await asyncio.sleep(2)
                    continue
                else:
                    print(f"❌ 第 {turn_number} 章生成失败：{e}")
                    return False
            except Exception as e:
                print(f"⚠️ 第 {turn_number} 章发生错误 (尝试 {attempt + 1}/{max_retries}): {e}")
                if attempt < max_retries - 1:
                    await asyncio.sleep(2)
                    continue
                else:
                    print(f"❌ 第 {turn_number} 章生成失败：{e}")
                    return False
        
        # 理论上不会到这里，但为了安全起见
        return False

def prepare_turn_data(history: list[dict], turn_number: int) -> dict[str, Any]:
    """为指定回合准备符合 prompt 格式的数据"""
    # 边界检查
    total_turns = len(history) // 2
    if turn_number < 1 or turn_number > total_turns:
        raise ValueError(f"turn_number {turn_number} 超出范围 [1, {total_turns}]")
    
    # 回合数从1开始，列表索引从0开始
    # 第 N 回合对应 history 列表中的 (N-1)*2 和 (N-1)*2 + 1 索引
    current_turn_index = (turn_number - 1) * 2
    
    # 确保索引有效（双重保险）
    if current_turn_index + 1 >= len(history):
        raise ValueError(f"history 数据不完整，无法获取第 {turn_number} 回合")
    
    user_content = history[current_turn_index]["content"]
    assistant_content = history[current_turn_index + 1]["content"]

    # 上下文：往前取7个回合（14条消息）
    context_before_start = max(0, current_turn_index - 14)
    context_turns_before = history[context_before_start:current_turn_index]

    # 上下文：往后取3个回合（6条消息）
    context_after_start = current_turn_index + 2
    context_after_end = context_after_start + 6
    context_turns_after = history[context_after_start:context_after_end]

    return {
        "context_turns_before": context_turns_before,
        "current_turn": {
            "user_content": user_content,
            "assistant_content": assistant_content
        },
        "context_turns_after": context_turns_after
    }

async def process_all_chapters(history: list[dict], turn_range: tuple[int, int], output_dir: str, concurrency_limit: int, model: str, base_url: str | None, usage_log_path: str):
    """并发处理所有指定的章节"""
    from tqdm.asyncio import tqdm_asyncio
    
    try:
        client = AsyncOpenAI(base_url=base_url)
        # 简单测试一下API Key是否有效
        await client.models.list(timeout=5)
        print("✅ OpenAI API Key 验证通过。")
    except Exception as e:
        print(f"❌ 连接 OpenAI API 失败: {e}")
        print("请检查您的 OPENAI_API_KEY 环境变量和网络连接。")
        return

    semaphore = asyncio.Semaphore(concurrency_limit)
    start_turn, end_turn = turn_range
    
    tasks = []
    for turn_number in range(start_turn, end_turn + 1):
        task = asyncio.create_task(process_single_chapter(
            client=client,
            semaphore=semaphore,
            history=history,
            turn_number=turn_number,
            output_dir=output_dir,
            model=model,
            usage_log_path=usage_log_path
        ))
        tasks.append(task)
    
    print(f"\n📊 开始处理 {len(tasks)} 个章节...")
    results = await tqdm_asyncio.gather(
        *tasks,
        desc="转写进度",
        total=len(tasks),
        unit="章"
    )
    
    success_count = sum(1 for r in results if r)
    fail_count = len(results) - success_count
    
    print("\n--- ✨ 转写完成 ---")
    print(f"✅ 成功: {success_count} 章")
    if fail_count > 0:
        print(f"❌ 失败: {fail_count} 章")
    print(f"所有文件已保存到 '{output_dir}' 目录。")
    print(f"📊 Token使用记录已保存到 '{usage_log_path}'")

def get_user_range(total_turns: int) -> tuple[int, int] | None:
    """获取用户输入的回合范围"""
    default_range = f"1-{total_turns}"
    while True:
        try:
            user_input = input(f"请输入要生成的章节范围 (例如 '{default_range}', 或 '5', 或直接回车生成全部): ").strip()
            if not user_input:
                return 1, total_turns

            if '-' in user_input:
                start_str, end_str = user_input.split('-')
                start = int(start_str.strip())
                end = int(end_str.strip())
            else:
                start = end = int(user_input.strip())

            if 1 <= start and start <= end and end <= total_turns:
                return start, end
            else:
                print(f"⚠️ 无效范围。请输入 1 到 {total_turns} 之间的数字。")
        except ValueError:
            print(f"⚠️ 无效输入。请输入正确的格式 (例如 '{default_range}' 或 '5')。")
        except Exception as e:
            print(f"❌ 发生未知错误: {e}")
            return None

def main():
    """主函数"""
    parser = argparse.ArgumentParser(
        description="将 AI GAME 会话历史批量转写为网络小说章节。",
        formatter_class=argparse.RawTextHelpFormatter
    )
    parser.add_argument("input_file", nargs='?', default=None, help="输入的 JSON 游戏历史文件路径 (可选)。")
    parser.add_argument("-o", "--output-dir", default="outputs", help="存放生成章节的输出目录 (默认: 'outputs')。")
    parser.add_argument("-c", "--concurrency", type=int, default=5, help="并发请求 OpenAI API 的最大数量 (默认: 5)。")
    parser.add_argument("--model", type=str, default="gpt-4o", help="指定使用的模型 (默认: 'gpt-4o')。")
    parser.add_argument("--base-url", type=str, default=None, help="指定 OpenAI API 的 base_url (可选)。")
    parser.add_argument("--usage-log", type=str, default="outputs/usage_log.json", help="Token使用记录的JSON文件路径 (默认: 'outputs/usage_log.json')。")
    
    args = parser.parse_args()

    input_file = args.input_file
    # 如果没有提供路径，则提示用户输入
    while not input_file or not os.path.exists(input_file):
        if input_file:
            print(f"❌ 错误: 找不到文件 '{input_file}'")
        
        user_input = input("请输入 JSON 游戏历史文件的路径 (或直接回车退出): ").strip().strip('\'"')
        if not user_input:
            print("👋 已取消操作。")
            return
        input_file = user_input

    if "OPENAI_API_KEY" not in os.environ:
        print("❌ 错误: 请设置 OPENAI_API_KEY 环境变量。")
        print("💡 提示: export OPENAI_API_KEY='your_key_here'")
        return

    # 加载历史文件
    try:
        with open(input_file, 'r', encoding='utf-8') as f:
            game_data = json.load(f)
    except (json.JSONDecodeError, IOError) as e:
        print(f"❌ 读取或解析 JSON 文件失败: {e}")
        return
    
    history = game_data.get("history", [])
    if not history or len(history) % 2 != 0:
        print("❌ 错误: JSON 中的 'history' 字段格式不正确或为空。它应该是 user/assistant 成对出现的列表。")
        return

    total_turns = len(history) // 2
    print(f"✅ 成功加载游戏历史: {os.path.basename(input_file)}，共 {total_turns} 个回合。")
    
    # 获取用户输入的范围
    turn_range = get_user_range(total_turns)
    if turn_range is None:
        return
    
    start_turn, end_turn = turn_range

    print(f"🚀 准备生成第 {start_turn} 到 {end_turn} 回合的章节 (共 {end_turn - start_turn + 1} 章)...")
    print(f"⚡ 并发数上限: {args.concurrency}")
    
    # 创建输出目录
    os.makedirs(args.output_dir, exist_ok=True)
    
    # 确保usage_log的目录存在
    usage_log_dir = os.path.dirname(args.usage_log)
    if usage_log_dir:
        os.makedirs(usage_log_dir, exist_ok=True)
    
    # 运行异步处理
    asyncio.run(process_all_chapters(
        history=history,
        turn_range=turn_range,
        output_dir=args.output_dir,
        concurrency_limit=args.concurrency,
        model=args.model,
        base_url=args.base_url,
        usage_log_path=args.usage_log
    ))

if __name__ == "__main__":
    # 提示用户需要安装的库
    try:
        import openai  # noqa: F401
        from tqdm.asyncio import tqdm_asyncio  # noqa: F401
    except ImportError:
        print("="*50)
        print("⚠️  检测到依赖库缺失！")
        print("请先运行以下命令安装所需库:")
        print("   pip install openai tqdm")
        print("="*50)
        exit(1)
    
    main()
